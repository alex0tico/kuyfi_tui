# docs/assets/

This folder holds static assets for the README and documentation.

## Planned files

| File | Description |
|---|---|
| `screenshot-attack-surface.png` | Terminal screenshot of the OSINT Scanner attack surface map after scanning a contract |
| `screenshot-chaos-report.png` | Terminal screenshot of a completed Chaos Monkey findings report |
| `demo.gif` | Animated GIF of a full session (already at `docs/demo.gif`) |
| `demo-video.mp4` | Full screen recording of a demo session (placeholder — link from README when available) |

## How to add screenshots

Run the TUI against a Testnet contract, then capture the terminal output:

```bash
# macOS — Command + Shift + 4, then drag over the terminal window
# Or use any terminal screenshot tool
```

Name the file descriptively and update the README to reference it.
